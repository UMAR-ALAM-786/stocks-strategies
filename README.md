# React + Vite

# 📈 Stock Trading Dashboard

```bash
# Quick Start
1. git clone https://github.com/yourusername/stock-trading-dashboard.git
2. npm create vite@latest stock-dashboard --template react

3. npm install react-chartjs-2 chart.js
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p

4. cd stocks strategies
5. npm install

6. npm run dev   # for Vite
# or
npm start     # for CRA


src/
├── App.jsx
└── components/
    ├── ui/
    │   ├── card.jsx
    │   ├── button.jsx
    │   └── input.jsx
    └── StockMetrics.jsx

